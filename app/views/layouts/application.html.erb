<!DOCTYPE html>
<html>
  <head>
    <title>PressStart</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>


    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

       <!-- Navigation -->
    <nav class="navbar navbar-default navbar-custom navbar-fixed-top navbar-inverse">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    Menu
                </button>
                <a class="navbar-brand">PressStart</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse navbar-right" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-left">
                  <li class="blog-nav-item"><%= link_to 'Home', root_path %></li>
                  <% if user_signed_in? %>
                    <!--This block is only visible to signed in users -->
                    <li class="blog-nav-item"><a href="/users/<%= current_user.id %>">
                  <% if current_user %>
                    <%= current_user.username %>
                  <% end %>
                  </a></li>
                    <li class="blog-nav-item"><%= link_to 'My Account', edit_user_registration_path  %></li>
                    <li class="blog-nav-item"><%= link_to 'Logout', destroy_user_session_path, :method => :delete %></li>
                     <li class="blog-nav-item"><a href="/users/:id/inbox">Chat</a></li>
                    <!-- The block ends here -->
                  <% else %>
                    <li class="blog-nav-item"><%= link_to 'Login', new_user_session_path  %></li>
                    <% end %>
                  </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>


	   <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
       
    <%= yield %>
  </body>
</html>
